---
mysql_backup:
  handling: rolling
  dumps_directory: "/home/{{ deploy_user }}/build_db_backups/{{ project_name }}/{{ build_type }}"
  databases:
    - database: "{{ project_name }}_{{ build_type }}"
      user: "{{ project_name }}_{{ build_type }}"
      credentials_file: "/etc/mysql/debian.cnf"