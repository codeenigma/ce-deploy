---
- name: Generates settings.php file.
  template:
    src: '{{ item }}'
    dest: "{{ deploy_path }}/{{ webroot }}/sites/{{ site.folder }}/settings.php"
  with_first_found:
    - "{{ deploy_path }}/{{ webroot }}/sites/{{ site.folder }}/settings.php.{{ build_type }}"
    - "settings.php.j2"
  become: yes